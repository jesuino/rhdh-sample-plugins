# Test Analytics Plugin

A very simple analytics dynamic plugin.

## Building

To build and run this you need `npm`:

```
npm install
yarn tsc
yarn build
yarn export-dynamic
```

## Running

In future we will have steps to install this on RHDH or Backstage Showcase.